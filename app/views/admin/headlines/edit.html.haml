%section#content.container
  .page-header
    = link_to admin_headlines_path, :class => 'btn' do
      %i.icon-list-alt
      Back
    = link_to [:admin, @headline], :class => 'btn btn-primary' do
      %i.icon-info-sign.icon-white
      Show
    %h1 Editing headline

  = render 'form'
