%section#content.container
  .page-header
    = link_to admin_headlines_path, :class => 'btn' do
      %i.icon-list-alt
      Back
    = link_to edit_admin_headline_path(@headline), :class => 'btn btn-primary' do
      %i.icon-pencil.icon-white
      Edit
    %h1 Listing headlines

  %dl
    %dt Title:
    %dd= @headline.title
    %dt Body:
    %dd= @headline.body
