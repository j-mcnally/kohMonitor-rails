%section#content.container
  .page-header
    = link_to admin_pages_path, :class => 'btn' do
      %i.icon-list-alt
      Back
    = link_to [:admin, @page], :class => 'btn btn-primary' do
      %i.icon-info-sign.icon-white
      Show
    %h1 Editing page

  = render 'form'
